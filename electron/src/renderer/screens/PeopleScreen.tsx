import { PeopleScreen as PeopleScreenImpl } from '../components/PeopleScreen';

export function PeopleScreen() {
  return (
    <main className="flex-1 overflow-hidden">
      <PeopleScreenImpl />
    </main>
  );
}
